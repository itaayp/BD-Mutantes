<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorYellow"
    tools:context=".EditarActivity">

    <EditText
        android:id="@+id/mutanteName"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_marginTop="50dp"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:layout_centerVertical="true"
        android:textColor="@color/colorBlue"
        android:layout_alignParentTop="true"
        android:hint="@string/nome_mutante"
        android:inputType="textPersonName" />

    <EditText
        android:id="@+id/mutanteSkills"
        android:layout_below="@id/mutanteName"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_margin="10dp"
        android:layout_centerVertical="true"
        android:textColor="@color/colorBlue"
        android:hint="@string/skills_mutante"
        android:inputType="textPersonName"/>

    <Button
        android:id="@+id/btnEditar"
        android:layout_below="@id/mutanteSkills"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_margin="10dp"
        android:layout_centerVertical="true"
        android:backgroundTint="@color/colorBlue"
        android:textColor="@color/colorYellow"
        android:onClick="editar"
        android:text="@string/salvar" />

    <Button
        android:id="@+id/btnVoltar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:backgroundTint="@color/colorBlue"
        android:textColor="@color/colorYellow"
        android:onClick="voltarListar"
        android:text="@string/voltar" />

    <Button
        android:id="@+id/btnExcluir"
        android:layout_below="@id/btnEditar"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_margin="10dp"
        android:layout_centerVertical="true"
        android:backgroundTint="@color/colorBlue"
        android:textColor="@color/colorYellow"
        android:onClick="excluir"
        android:text="@string/excluir" />
</RelativeLayout>